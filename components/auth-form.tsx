"use client";

import { useState } from "react";
import { signIn, signUp } from "@/app/auth/actions";

export function AuthForm({error,message}:{error?:string;message?:string}){const [mode,setMode]=useState<"login"|"signup">("login");return <div className="mx-auto max-w-md"><p className="eyebrow">Your REGEAR account</p><h1 className="display mt-4 text-6xl">{mode==="login"?"Welcome back.":"Join the edit."}</h1><div className="mt-8 flex border-b hairline text-sm"><button onClick={()=>setMode("login")} className={`flex-1 pb-3 ${mode==="login"?"border-b-2 border-black font-semibold":"opacity-45"}`}>Sign in</button><button onClick={()=>setMode("signup")} className={`flex-1 pb-3 ${mode==="signup"?"border-b-2 border-black font-semibold":"opacity-45"}`}>Create account</button></div><form action={mode==="login"?signIn:signUp} className="mt-7 grid gap-5">{mode==="signup"&&<Field label="Name" name="name"/>}<Field label="Email" name="email" type="email"/><Field label="Password" name="password" type="password"/><button className="mt-2 bg-[var(--ink)] py-4 text-white">{mode==="login"?"Sign in":"Create account"}</button></form>{error&&<p className="mt-4 text-sm text-red-700">{error}</p>}{message&&<p className="mt-4 text-sm text-green-700">{message}</p>}<p className="mt-6 text-xs leading-5 opacity-50">An account lets you save pieces and see your order history. Guest checkout remains available.</p></div>}
function Field({label,name,type="text"}:{label:string;name:string;type?:string}){return <label className="grid gap-2 text-xs font-medium">{label}<input required name={name} type={type} minLength={type==="password"?8:undefined} className="border-b hairline bg-transparent px-1 py-3 text-base outline-none focus:border-black"/></label>}
